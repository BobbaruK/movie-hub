import styles from "./ByLanguage.module.scss";

const ByLanguage = () => {
  return (
    <>
      <h3 className={[styles.filterLanguageTitle].join(" ")}>ByLanguage</h3>
    </>
  );
};

export default ByLanguage;
